\RequirePackage{luatex85}
\documentclass[tikz]{standalone}
\begin{document}
\begin{tikzpicture}[line width=0.03in]
\clip (0,0) rectangle (8.5in,11in);
\foreach \row in {0,...,2}{
\foreach \col in {0,...,2}{
\foreach \bar in {0,...,5}{
\draw ({0.35in+\col*0.4in}, {0.365in+\bar*0.039in+\row*0.4in}) coordinate (xy);
\pgfmathtruncatemacro\barval{mod(floor(mod(\col+20*\row,64)/2^\bar),2)}
\ifnum\barval=0
\draw (xy) -- ++(right:0.1in) ++(right:0.1in) -- ++(right:0.1in);
\else\draw (xy) -- ++(right:0.3in);\fi
}}}
\end{tikzpicture}
\end{document}